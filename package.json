{
  "name": "solidt",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "pnpm --filter sys dev",
    "start:dev": "pnpm --stream -r run dev",
    "build": "pnpm --stream -r && --filter sys run build",
    "clean": "rm -rf .parcel-cache && pnpm -r run clean",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "dayjs": "^1.11.11",
    "dlibs": "workspace:*",
    "sys": "workspace:*"
  }
}
